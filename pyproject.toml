[project]
name = "ai-money-mentor"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "flask",
    "pandas",
    "numpy",
    "scikit-learn",
    "joblib"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
